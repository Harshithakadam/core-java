what is java?
java is a high level object oriented programming language which is used to built the programs.

explain how java execution?
java execuits the coding by converting source code to byte code that is done by the jvm
example:java fileName

what is data type?
data type means,it tell what kind of valuves need to be stored in the variable and it also define the type and size of the variable.

explian types of data type?
there are 2 types 
1.primitive data type:they are the data types which are pre-defined or built in which are provided by the java developers .
there are 8 types:
integer: int a=10;
float: float a=10.0f;
double: double a=10.00;
char: char 'a'=1;
short
long: long phone=12345678901l;
boolean: boolean a=true;
byte: byte a=123;
2.non primitive data type:they are also a type of data type but which are developed by the users according to thier needs .
types:
String:it is a collection of spical characters.
String name="Ananya";
Array:it contains multiple values in a single variable.
int age[]={12,20,14};

what is variable?
it is a conatainer used to store data and the values can be changed during the execution
types of variables:
1.local:it is declared inside the class and method.
2.static: it is declared inside the class but out side the method by using static keyword.
3.instance: it is declared inside the class but out side the method without using static keyword.
4.parameteraised:it is a variable where the values are declared inside the parameters.

what is array?
array is a collection os homogenious element.
it can contain multiple values in a single variable.